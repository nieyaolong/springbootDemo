spring:

  datasource:
    url: jdbc:mysql://localhost/springDemo
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    database: mysql
    show-sql: true
server:
  port: 9000